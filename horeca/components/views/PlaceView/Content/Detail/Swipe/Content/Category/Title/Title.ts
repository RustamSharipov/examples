import styled from 'styled-components'

import { snippets } from 'theme'

export default styled.div`
  position: absolute;
  z-index: 1;
  left: 2rem;
  top: 10rem;
  font-size: 3rem;
  ${snippets.textShadow}
`

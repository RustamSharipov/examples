import styled from 'styled-components'

import { ExpandControl } from 'components/shared'

export default styled(ExpandControl)`
  position: absolute;
  z-index: 10;
  right: 2rem;
  transition: bottom 0.25s;
  pointer-events: all;
`

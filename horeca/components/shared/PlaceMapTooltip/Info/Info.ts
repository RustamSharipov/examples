import styled from 'styled-components'

export default styled.div`
  display: flex;
  align-items: center;
  padding: 0 1rem;
  color: var(--color-primary-dark);
  font-size: 1.5rem;
  white-space: nowrap;
`
